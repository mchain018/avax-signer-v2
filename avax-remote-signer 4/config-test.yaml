server:
  address: "0.0.0.0:9090"
  tls:
    enabled: false  # Disabled for local testing

backend:
  type: "file"
  config:
    key_path: "keys/test-validator-key.json"

logging:
  level: "info"
  format: "console"
